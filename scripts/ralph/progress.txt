## Codebase Patterns
- Spin calculation must be extracted to pure engine modules (no React Native dependencies) for testability
- iOS uses `react-native-reanimated` shared values for rotation; web uses React state — spin formulas must produce the same result regardless of animation framework
- WHEEL_CONFIG has 24 wedges, WEDGE_ANGLE = 15 degrees
- SeededRNG(seed).range(0, 24) determines the winning wedge index deterministically
- Global git pre-commit hook at `~/.config/pre-commit/hooks/pre-commit` does secret scanning; may need `-c core.hooksPath=/dev/null` for clean commits
- Pre-existing type error in game.test.ts (`difficultyRating` should be `difficulty`) — fixed in prior run
- GameAction union type is defined in `game.ts` (not `types.ts`) alongside INITIAL_STATE and gameReducer
- iOS WheelWedge uses type `'VALUE'` where web uses `'CASH'` — any code ported from web that switches on wedge type must update this
- Navigation is state-based (`activeScreen` useState in StandardModeApp) — no routing library. Icons from `lucide-react-native` (ChevronLeft, Play, BookOpen, BarChart3)
- ALL_PACKS from `packs.ts` provides all available packs as `PuzzlePack[]`. Includes original, seasons 1-20, seasons 40-42, individual season packs, difficulty sub-packs, and random quick-game packs
- PackBrowser accepts `packs`, `activePackId`, and `onSelectPack` props — parent controls pack selection behavior and navigation
- `SharedValue` type: import as named import from `react-native-reanimated`, NOT `Animated.SharedValue`
- Reanimated frame cycling: `withRepeat(withTiming(FRAME_COUNT, { duration, easing: Easing.linear }), -1, false)` + `Math.floor(value) % FRAME_COUNT` for discrete frames
- VOWELS and CONSONANTS constants are exported from `ios/src/engine/types.ts`
- TurnState is defined inline in GameState type in `ios/src/engine/types.ts` (union of string literals)
- TOSS_UP_TICK now accepts { dtMs: number } — time-based accumulation with multi-reveal loop
- START_ROUND sets turnState based on round_type: TOSSUP → 'TOSSUP_REVEALING', BONUS → 'BONUS_PICKING', MAIN → 'IDLE'
- TOSS_UP_TICK lockout→reveal transition uses recursive reducer call to apply leftover time
- START_ROUND already handles BONUS (reveals RSTLNE) and TOSSUP (shuffles reveal order) — extend, don't replace
- StandardModeApp.tsx is 900+ lines — the main game UI component with useReducer state management
- Haptics already imported in StandardModeApp via expo-haptics
- Solve modal uses Modal component with TextInput and keyboardShouldPersistTaps='handled'
- `getPuzzlesForMode(puzzles, mode)` exported from `packs.ts` filters puzzles by allowed_modes; puzzles without allowed_modes are treated as eligible for all modes
- BONUS_EXCLUDED_CATEGORIES in packs.ts uses case-insensitive matching for category exclusion
- BONUS_CHOOSE_LETTERS validates: 3 distinct consonants (not RSTLNE) + 1 vowel (not E); returns state unchanged on invalid input
- Only 4 vowels are eligible for bonus picks: A, I, O, U (E is in RSTLNE)
- BONUS_SOLVE_ATTEMPT returns state unchanged on wrong solve (unlimited retries within timer); TOSS_UP_SOLVE_ATTEMPT triggers lockout on wrong solve
- BONUS_TICK handler is simple decrement (not a loop like TOSS_UP_TICK) — timer just counts down to 0
- Theme `colors.yellow` only has shades 200-500 (no 600) — use `colors.orange[500]/[600]` for gradient pairs
- `startMode(mode)` in StandardModeApp: sets selectedRoundMode, filters puzzles via getPuzzlesForMode, overrides puzzle round_type, and navigates to game — single callback for mode selection
- Theme `colors.orange` only has 500/600/700 (no 400) — use 500 for text. `colors.slate` only has 400-900 (no 300)
- Toss-Up tick interval uses `setInterval` at ~33ms with `Date.now()` delta tracking for accurate dtMs
- Toss-Up solve modal is separate from standard solve modal — uses TOSS_UP_SOLVE_ATTEMPT action, not SOLVE_ATTEMPT
- `isTossUpMode` flag (`selectedRoundMode === 'TOSSUP'`) gates all toss-up UI rendering
- Round over celebration: wins get confetti + Vanna + 3s delay, losses get immediate Next Puzzle button with error haptic
- TOSSUP_BUZZED state opens solve modal; modal close without submission keeps the buzzed state (reducer doesn't transition back)
- Keyboard `selectedLetters` prop: shows letters with green highlight and allows tapping for deselection; used for bonus round picking
- `isBonusPicking` flag (`isBonusMode && state.turnState === 'BONUS_PICKING'`) gates bonus picking UI rendering
- `isBonusMode` flag (`selectedRoundMode === 'BONUS'`) used for bonus-specific round over text and hiding standard game controls
- Bonus picking local state: `bonusSelectedConsonants[]` (max 3) and `bonusSelectedVowel` (null or string) — cleared on nextRound and on confirm
- RSTLNE letters are already in `state.guessedLetters` after START_ROUND for BONUS — no need to build a separate disabled list

# Ralph Progress Log
Started: Sun Jan 26 2026 — Toss-Up & Bonus Round Modes
---

## 2026-01-26 - US-001
- Extended TurnState union with 5 new states: TOSSUP_REVEALING, TOSSUP_BUZZED, TOSSUP_LOCKED_OUT, BONUS_PICKING, BONUS_SOLVE_TIMER
- Added tossUpElapsedMs, tossUpRevealIntervalMs, tossUpLockoutMs, tossUpLockoutDurationMs to GameState
- Added bonusTimerMs, bonusTimerDurationMs, roundResult to GameState
- Added allowed_modes optional field to Puzzle interface
- Updated INITIAL_STATE with all new defaults
- Updated START_ROUND to reset new fields (tossUpElapsedMs, tossUpLockoutMs, bonusTimerMs, bonusPicks, roundResult)
- Files changed: ios/src/engine/types.ts, ios/src/engine/game.ts
- **Learnings for future iterations:**
  - Typecheck passes with no errors, all 222 existing tests pass
  - START_ROUND needed updating to reset new fields on each round start
  - The bonusTimer (old field) is still present alongside new bonusTimerMs — kept for backward compat
---

## 2026-01-26 - US-002
- Updated mapPuzzles() to set allowed_modes on each puzzle based on category
- Added BONUS_EXCLUDED_CATEGORIES constant with case-insensitive matching: 'Before & After', 'Same Name', 'Rhyme Time', 'Song Lyrics', 'Title/Author'
- Excluded categories get allowed_modes: ['MAIN', 'TOSSUP'], all others get ['MAIN', 'TOSSUP', 'BONUS']
- Added and exported getPuzzlesForMode(puzzles, mode) that filters by allowed_modes, defaulting to all-eligible if undefined
- Created ios/src/__tests__/puzzleModes.test.ts with 9 tests
- Files changed: ios/src/engine/packs.ts, ios/src/__tests__/puzzleModes.test.ts
- **Learnings for future iterations:**
  - getAllowedModes() helper is a pure function — easy to test in isolation
  - getPuzzlesForMode is exported from packs.ts (same module as ALL_PACKS)
  - Typecheck passes, all 231 tests pass (222 existing + 9 new)
---

## 2026-01-26 - US-003
- Enhanced TOSS_UP_TICK action to accept { dtMs: number } for time-based letter reveal
- Updated START_ROUND to set turnState to 'TOSSUP_REVEALING' for TOSSUP puzzles and 'BONUS_PICKING' for BONUS puzzles
- TOSS_UP_TICK handler: accumulates elapsed time, reveals letters when elapsed >= 1000ms interval
- Multi-reveal loop: large dtMs reveals multiple letters in one dispatch (for background resume)
- Lockout handling: decrements tossUpLockoutMs, transitions back to TOSSUP_REVEALING with leftover time via recursive reducer call
- Round end: when all letters in tossUpRevealOrder revealed, sets ROUND_OVER + roundResult 'loss' + reveals all positions
- No-op when turnState is not TOSSUP_REVEALING or TOSSUP_LOCKED_OUT
- Created ios/src/__tests__/tossUpEngine.test.ts with 19 tests
- Files changed: ios/src/engine/game.ts, ios/src/__tests__/tossUpEngine.test.ts
- **Learnings for future iterations:**
  - When last toss-up letter is revealed, revealedPositions is replaced with ALL phrase positions (including spaces), not just letter positions — tests must account for this when verifying reveal order
  - Recursive reducer call for lockout→reveal transition handles leftover time cleanly without separate loop
  - The multi-reveal loop already satisfies US-014 requirements (large dtMs handling)
  - Typecheck passes, all 250 tests pass (231 existing + 19 new)
---

## 2026-01-26 - US-004
- Added BUZZ_IN action: transitions from TOSSUP_REVEALING to TOSSUP_BUZZED; no-op in all other states
- Added TOSS_UP_SOLVE_ATTEMPT action: correct solve (case-insensitive) → ROUND_OVER + win; wrong solve → TOSSUP_LOCKED_OUT with 3000ms lockout
- Lockout prevents buzz-in; existing TOSS_UP_TICK lockout handler from US-003 resumes revealing after expiry
- 12 new tests: buzz-in pauses reveal, preserves state, correct/wrong solve, lockout flow, no-op guards
- Files changed: ios/src/engine/game.ts, ios/src/__tests__/tossUpEngine.test.ts
- **Learnings for future iterations:**
  - BUZZ_IN and TOSS_UP_SOLVE_ATTEMPT are separate from the standard SOLVE_ATTEMPT — toss-up has its own solve flow
  - Wrong toss-up solve sets tossUpLockoutMs = tossUpLockoutDurationMs (3000) — the TOSS_UP_TICK handler from US-003 handles the lockout countdown and resume
  - TOSS_UP_SOLVE_ATTEMPT is a no-op when not TOSSUP_BUZZED; BUZZ_IN is a no-op when not TOSSUP_REVEALING
  - Typecheck passes, all 262 tests pass (250 existing + 12 new)
---

## 2026-01-26 - US-005
- Added 2 end-to-end simulation tests to ios/src/__tests__/tossUpEngine.test.ts
- Simulation 1 (win path): start round → tick 3 reveals → buzz in → wrong solve → lockout → tick through lockout (in two steps) → resume → tick 1 more reveal → buzz in → correct solve → round over win. Verifies intermediate state at every step (turnState, revealedPositions count, tossUpIndex, roundResult, tossUpLockoutMs)
- Simulation 2 (loss path): start round → tick letters one at a time verifying each intermediate state → last letter triggers ROUND_OVER with roundResult 'loss' and all positions revealed
- Both tests use fixed seed 42 with puzzle "HELLO WORLD" for deterministic reveal order
- Files changed: ios/src/__tests__/tossUpEngine.test.ts
- **Learnings for future iterations:**
  - The existing helper functions (makeTossUpPuzzle, expectedRevealOrder, startTossUpRound) make simulation tests straightforward — no new helpers needed
  - When lockout expires with exactly 0ms leftover, no additional reveals occur — the recursive reducer call with dtMs=0 is a no-op since 0 < 1000ms interval
  - Typecheck passes, all 264 tests pass (262 existing + 2 new)
---

## 2026-01-26 - US-006
- Added BONUS_CHOOSE_LETTERS action to GameAction union: { type: 'BONUS_CHOOSE_LETTERS'; consonants: [string, string, string]; vowel: string }
- Implemented BONUS_CHOOSE_LETTERS handler in gameReducer:
  - Only works when turnState is 'BONUS_PICKING'
  - Validates exactly 3 distinct consonants (must be in CONSONANTS, not in RSTLNE)
  - Validates exactly 1 vowel (must be in VOWELS, not in RSTLNE — effectively rejects 'E')
  - Case-insensitive input handling (uppercases before validation)
  - On valid: adds chosen letters to guessedLetters, reveals all matching positions, sets bonusPicks, transitions to BONUS_SOLVE_TIMER
  - On invalid: returns state unchanged (referential identity)
- Created ios/src/__tests__/bonusEngine.test.ts with 20 tests covering:
  - START_ROUND BONUS behavior (6 tests): turnState, RSTLNE reveal, guessedLetters, timer reset, roundResult, bonusPicks
  - BONUS_CHOOSE_LETTERS (14 tests): valid picks, letter reveal positions, bonusPicks, RSTLNE rejection (R, T, E), duplicates, non-consonant in consonant slot, non-vowel in vowel slot, invalid chars, no-op guards, case-insensitive input, RSTLNE position preservation
- Files changed: ios/src/engine/game.ts, ios/src/__tests__/bonusEngine.test.ts
- **Learnings for future iterations:**
  - CONSONANTS imported from types.ts alongside VOWELS — both needed for validation
  - RSTLNE validation catches R,S,T,L,N (consonants) and E (vowel) — the vowel 'E' is the only RSTLNE vowel, so only 4 vowels are eligible picks (A, I, O, U)
  - Referential identity check (`next === state`) works for verifying no-op returns since reducer returns `state` directly on invalid input
  - bonusPicks stores the 4 chosen letters (not RSTLNE) — RSTLNE is in guessedLetters from START_ROUND
  - Typecheck passes, all 284 tests pass (264 existing + 20 new)
---

## 2026-01-26 - US-007
- Added BONUS_TICK action: { type: 'BONUS_TICK'; dtMs: number } to GameAction union
- Added BONUS_SOLVE_ATTEMPT action: { type: 'BONUS_SOLVE_ATTEMPT'; phrase: string } to GameAction union
- Implemented BONUS_TICK handler: decrements bonusTimerMs by dtMs, expires round as loss when timer reaches 0, reveals all positions on expiry, no-op when not in BONUS_SOLVE_TIMER
- Implemented BONUS_SOLVE_ATTEMPT handler: correct solve (case-insensitive) → ROUND_OVER + win + all revealed, wrong solve → state unchanged (allows retry), no-op when not in BONUS_SOLVE_TIMER
- Added 12 new tests to ios/src/__tests__/bonusEngine.test.ts covering: timer decrement, timer expiry, overshoot expiry, no-op guards, accumulated ticks, correct solve, case-insensitive solve, wrong solve retry, multiple retries then win, no-op when wrong turnState
- Files changed: ios/src/engine/game.ts, ios/src/__tests__/bonusEngine.test.ts
- **Learnings for future iterations:**
  - BONUS_SOLVE_ATTEMPT returns `state` (referential identity) on wrong solve — unlike TOSS_UP_SOLVE_ATTEMPT which triggers lockout, bonus allows unlimited retries within timer
  - BONUS_TICK sets bonusTimerMs to 0 (not negative) on expiry for clean state
  - enterSolveTimer() helper chains startBonusRound() + BONUS_CHOOSE_LETTERS to quickly reach BONUS_SOLVE_TIMER state in tests
  - Typecheck passes, all 296 tests pass (284 existing + 12 new)
---

## 2026-01-26 - US-008
- Added 3 end-to-end simulation tests to ios/src/__tests__/bonusEngine.test.ts in a new "Bonus Engine: end-to-end simulation" describe block
- Simulation 1 (win path): start round → verify RSTLNE positions [1,4,7,12,13,16,17,19,21] revealed → choose C,D,M,A → verify positions [2,3,6,9,10,20] added → tick 10s → correct solve → ROUND_OVER win
- Simulation 2 (loss path): start round → choose letters → tick 8s+7s+5s (incremental) → timer hits 0 → ROUND_OVER loss → verify further ticks/solves are no-ops
- Simulation 3 (retry path): start round → choose letters → tick 3s → wrong solve (state unchanged) → tick 5s → another wrong solve (state unchanged) → correct solve → ROUND_OVER win with bonusTimerMs frozen at 12000
- All tests use puzzle "PRACTICE MAKES PERFECT" with manually computed RSTLNE positions and chosen letter positions
- Files changed: ios/src/__tests__/bonusEngine.test.ts
- **Learnings for future iterations:**
  - "PRACTICE MAKES PERFECT" has R,S,T,E appearing but L,N do not — RSTLNE_POSITIONS = [1,4,7,12,13,16,17,19,21] (9 positions)
  - bonusTimerMs is frozen at its last value when BONUS_SOLVE_ATTEMPT succeeds (timer is not reset to 0 on win)
  - BONUS_SOLVE_ATTEMPT and BONUS_TICK are both no-ops after ROUND_OVER — safe to verify post-round stability
  - Typecheck passes, all 299 tests pass (296 existing + 3 new simulation tests)
---

## 2026-01-26 - US-009
- Added `selectedRoundMode: RoundType` state (default 'MAIN') to StandardModeApp
- Replaced single Play card with 3 mode cards: Standard Game (Play icon, green), Toss-Up (Zap icon, orange), Bonus Round (Trophy icon, purple)
- Each mode card calls `startMode(mode)` which sets selectedRoundMode, filters puzzles via `getPuzzlesForMode()`, overrides puzzle's `round_type` to the selected mode, and navigates to 'game' screen
- Updated `nextRound` to use `getPuzzlesForMode` and override `round_type` with `selectedRoundMode`
- Kept Puzzle Packs and Strategy cards on home screen (blue and slate gradients)
- Imported `Zap`, `Trophy` from lucide-react-native; `RoundType` from types; `getPuzzlesForMode` from packs
- Files changed: ios/src/components/StandardModeApp.tsx
- **Learnings for future iterations:**
  - `colors.yellow` only has 200-500 shades (no 600) — use `colors.orange` for gradient pairs needing 500+600
  - `startMode()` pattern: set mode state + filter puzzles + override round_type + navigate in one callback
  - The `round_type` override on the puzzle object is needed because all puzzles in pack data default to 'MAIN' — the mode selector determines the actual game mode
  - Typecheck passes, all 299 tests pass (no new tests needed — UI story)
---

## 2026-01-26 - US-010
- Implemented Toss-Up UI screen in StandardModeApp.tsx
- Added toss-up tick interval (setInterval at ~33ms with Date.now() delta tracking)
- Added Buzz In button with Zap icon, orange gradient, centered below puzzle board
- Added toss-up solve modal (reuses Modal component pattern with TextInput)
- Added lockout countdown display showing remaining seconds
- Added TOSS-UP status banner with context text (Buzz in, Locked out, Correct, Puzzle Revealed)
- Hid wheel and standard keyboard when in TOSSUP mode
- Updated round-over section to show win/loss-specific messages for toss-up
- Win: confetti + Vanna + 3s delay before Next Puzzle; Loss: immediate Next Puzzle with error haptic
- Wired expo-haptics: Heavy impact on buzz-in, Success/Error notification on solve result
- Clear tick interval on unmount and when round ends (via turnState dependency in useEffect)
- Files changed: ios/src/components/StandardModeApp.tsx
- **Learnings for future iterations:**
  - Theme `colors.orange` only has 500/600/700 (no 400) — use 500 for text colors
  - Theme `colors.slate` only has 400-900 (no 300) — use 400 for subtle text
  - Toss-Up solve modal is separate from standard solve modal (different dispatch action, different styling)
  - `isTossUpMode` flag gates rendering — avoids mixing standard/toss-up UI logic with complex conditionals
  - TOSSUP_BUZZED opens the solve modal; if user cancels, state stays TOSSUP_BUZZED (reducer needs BUZZ_IN from TOSSUP_REVEALING)
  - Round over celebration logic split: `roundResult === 'win'` or `null` (standard mode) shows confetti; `roundResult === 'loss'` shows error haptic + immediate next puzzle
  - Typecheck passes, all 299 tests pass (no new tests needed — UI story)
---

## 2026-01-26 - US-011
- Implemented Bonus Round letter picking UI in StandardModeApp.tsx
- Added local state: bonusSelectedConsonants (string[], max 3) and bonusSelectedVowel (string | null)
- Added isBonusMode and isBonusPicking flags for conditional rendering
- Bonus picking area shows: BONUS ROUND banner, pick progress indicator (consonants: X/3, vowel: Y), keyboard with RSTLNE disabled, Confirm button
- Keyboard extended with `selectedLetters` prop: renders selected keys with green background, allows deselection by tapping
- Enforced picking order: consonants first (3), then vowel (1) — vowel keys only respond when 3 consonants selected
- highlightVowels activates when 3 consonants selected and no vowel yet (pulse animation on eligible vowels)
- Confirm button dispatches BONUS_CHOOSE_LETTERS, clears local selection state
- Haptic feedback: Light impact on each letter pick, Medium impact on confirm
- Round over text updated for bonus mode: shows "TIME'S UP!" for losses
- Selection state cleared on nextRound
- Added placeholder bonusSolveArea for US-012 (timed solve phase)
- Files changed: ios/src/components/StandardModeApp.tsx, ios/src/components/Keyboard.tsx
- **Learnings for future iterations:**
  - RSTLNE letters are already in state.guessedLetters from START_ROUND — just pass guessedLetters as disabled letters directly
  - Keyboard selectedLetters prop works alongside guessedLetters: selected letters override guessed styling and remain tappable
  - Bonus picking is a separate rendering branch from toss-up and standard — uses isBonusPicking (mode + turnState) as gate
  - bonusSolveArea is a placeholder for US-012 — renders when isBonusMode but not isBonusPicking and not isRoundOver
  - Typecheck passes, all 299 tests pass (no new tests needed — UI story)
---
